apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}{{.Values.serviceName}}
  namespace: {{.Release.Namespace}}
spec:
  selector:
    env:  {{.Release.Name}}-{{.Values.environment}}
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432 
